#include "main.h"

/**
 * _isupper - function for upper case characters
 *
 * @c: variable
 * Return: (0)
 */
int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
