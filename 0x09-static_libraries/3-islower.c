#include "main.h"
/**
 * _islower - prog that checks for lowercase character
 * @c: the character
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
